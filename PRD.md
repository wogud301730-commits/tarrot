# PRD.md — 제품 요구사항 문서

| 항목 | 내용 |
|---|---|
| 프로젝트명 (가칭) | **루나타로 (LUNA TAROT)** — 확정 브랜드명 아님, 자유롭게 변경 가능 |
| 작성일 | 2026-08-10 |
| 상태 | 초안 (MVP 개발 착수용) |
| 대상 독자 | 비전공자 포함 — 전문 용어는 최대한 풀어서 설명 |

이 문서는 "무엇을 만드는가"를 정의합니다. "어떻게 작업하는가"는 [`AGENTS.md`](./AGENTS.md), 세부 구현 방식은 `docs/` 폴더를 참고하세요.

---

## 1. 배경 및 문제 정의

사용자는 복잡한 회원가입이나 앱 설치 없이, 웹페이지에 들어가서 **몇 번의 터치만으로 타로 카드를 뽑고 그 의미를 이해하기 쉬운 설명으로 받아볼 수 있는 경험**을 원합니다.

다만 AI가 타로를 해석할 때 흔히 생기는 문제는 "그럴듯하지만 근거 없는 이야기를 지어내는 것"입니다. 이 프로젝트는 이를 방지하기 위해, **AI가 검증된 표준 타로 카드 의미(레퍼런스 데이터)를 벗어나지 않고 해석하도록 강제하는 구조**를 핵심 요구사항으로 둡니다.

## 2. 목표 (Goals)

- G1. 모바일 브라우저에서 30초 이내에 "카드 뽑기 → 해석 읽기"까지 완료할 수 있다.
- G2. AI 해석이 신뢰할 수 있는 표준 타로 의미 체계에 근거하며, 참고자료에 없는 내용을 지어내지 않는다.
- G3. 명품 브랜드 웹사이트 수준의 고급스러운 무드와, 모바일 앱 특유의 손맛 나는 터치 인터랙션을 결합한 UI를 제공한다.
- G4. 운영 비용(OpenAI 호출 비용)을 예측 가능한 범위로 통제한다.

## 2.1 목표가 아닌 것 (Non-goals, MVP 기준)

- 실시간 유료 상담사 연결, 결제/구독
- 회원가입/로그인 기반 개인화 (Phase 2로 이연 — §10 참조)
- 사주/신점 등 타로 외 점술 콘텐츠
- 네이티브 앱(iOS/Android) 출시

---

## 3. 타겟 사용자 & 시나리오

- **주 사용자**: 가볍게 하루 운세/현재 고민에 대한 힌트를 얻고 싶은 일반인 (타로 지식 없음 가정).
- **대표 시나리오**: 사용자가 링크를 열고 → 배경 연출을 잠깐 감상 → "터치하여 카드 뽑기" → 스프레드(1장 또는 3장) 선택 → 카드를 터치해 뒤집음 → AI가 해석을 생성하는 동안 로딩 연출 → 결과 화면에서 카드 의미와 해석을 읽음 → (선택) 다시 뽑기.

## 4. 핵심 사용자 플로우

```
[랜딩/인트로 연출]
      ↓ 터치
[스프레드 선택 화면]  (1장 스프레드 / 3장 스프레드 — 과거·현재·미래)
      ↓ 선택
[질문 입력 (선택 사항, 건너뛰기 가능)]
      ↓
[카드 덱 화면] — 카드 뒷면이 부채꼴/그리드로 표시, 사용자가 필요한 장수만큼 터치해 선택
      ↓ 터치할 때마다 카드 플립 애니메이션
[로딩 연출] — 서버가 카드 확정 + AI 해석 생성
      ↓
[결과 화면] — 카드 이미지, 카드명, 정/역방향, 키워드, AI 해설 텍스트, "다시 뽑기" 버튼
      ↓ (선택)
[카드 사전 화면] — 78장 카드 의미를 목록에서 열람 (리딩과 무관하게 언제든 접근 가능)
```

## 5. 기능 요구사항 (Functional Requirements)

각 항목은 **범위 / 제약조건 / 완료 기준 / 검증 기준**을 포함합니다. 세부 구현은 `docs/` 해당 문서를 따릅니다.

### FR-1. 인트로/랜딩 연출
- **범위**: 첫 진입 시 전체 화면 배경 연출(애니메이션 배경 + "터치하여 시작" 유도 문구)을 보여준다. 디자인 상세는 `docs/05-ui-ux-design-spec.md` §1.
- **제약**: 이미지/영상 에셋 없이 CSS/SVG/Canvas만으로 구현 (초기 로딩 속도 확보). 3초 이상 사용자가 아무 조작 없어도 자동으로 다음 화면으로 넘어가지 않는다(사용자 주도 진행).
- **완료 기준**: 375px~1280px 폭에서 레이아웃 깨짐 없이 표시되고, 터치/클릭 시 스프레드 선택 화면으로 전환된다.
- **검증 기준**: 실기기 또는 브라우저 모바일 에뮬레이션에서 3초 내 첫 콘텐츠(FCP)가 표시되는지 확인.

### FR-2. 스프레드 선택
- **범위**: "1장 스프레드", "3장 스프레드(과거/현재/미래)" 두 종류 제공. 데이터 출처는 `spreads` 테이블(`docs/02`).
- **제약**: 스프레드 종류를 프론트엔드 코드에 하드코딩하지 않고 `GET /api/spreads` 응답으로 렌더링한다 (`docs/03`).
- **완료 기준**: 두 스프레드 중 하나를 선택하면 다음 단계(질문 입력)로 이동하고 선택값이 세션 상태에 유지된다.
- **검증 기준**: 스프레드를 서버 데이터에서 하나 삭제/추가했을 때 프론트엔드 수정 없이 화면에 반영되는지 확인.

### FR-3. 질문 입력 (선택)
- **범위**: 최대 200자, 선택 입력. 비워도 진행 가능.
- **제약**: 입력값은 서버에 전송되어 AI 프롬프트에 포함될 수 있으나, 원문 그대로 별도 로그 파일에 남기지 않는다 (`docs/02` §개인정보).
- **완료 기준**: 입력 없이도, 입력 후에도 정상적으로 다음 단계 진행 가능.
- **검증 기준**: 200자 초과 입력 시 그 이상 입력되지 않거나 명확한 경고가 표시된다.

### FR-4. 카드 덱 인터랙션 (터치로 뽑기)
- **범위**: 스프레드에서 정한 장수만큼, 사용자가 화면의 카드 뒷면들을 직접 터치해서 선택하는 연출. 실제 카드 값은 서버에서 결정.
- **제약**: 클라이언트는 "몇 번째 카드를 선택했는지(위치)"만 서버에 알리고, 그 위치에 어떤 카드가 배정될지는 절대 클라이언트에서 계산하지 않는다.
- **완료 기준**: 카드를 터치하면 플립 애니메이션과 함께 카드 앞면(서버가 확정한 결과)이 표시된다. 필요한 장수를 모두 선택하면 자동으로 결과 화면으로 이동한다.
- **검증 기준**: 같은 스프레드로 10회 이상 반복 시 카드/정역방향 분포가 매번 달라짐을 확인(고정값 하드코딩 여부 점검).

### FR-5. AI 해석 생성
- **범위**: 뽑힌 카드(들)과 스프레드 위치, 질문(있다면)을 바탕으로 OpenAI가 자연스러운 해설 텍스트를 생성.
- **제약**: 반드시 `docs/04-ai-interpretation-guide.md`의 그라운딩 프롬프트 구조를 사용. 카드 의미의 출처는 `data/seed-cards.json`.
- **완료 기준**: 결과 화면에 각 카드별 해설과 전체 종합 메시지가 표시되며, 응답 시간은 평균 10초 이내(타임아웃 20초).
- **검증 기준**: `docs/04` §검증 기준의 샘플 점검 절차를 통과.

### FR-6. 결과 화면
- **범위**: 카드 이미지(또는 상징 아이콘), 카드명(한글/영문), 정방향/역방향 표시, 키워드 태그, AI 해설, 안전 디스클레이머 문구, "다시 뽑기"/"카드 사전 보기" 버튼.
- **완료 기준**: 스크린 리더로 카드명과 해설을 읽을 수 있도록 시맨틱 마크업 구성.
- **검증 기준**: Lighthouse 접근성 점수 90점 이상.

### FR-7. 카드 사전 (78장 브라우징)
- **범위**: 리딩 여부와 무관하게 78장 카드 목록/검색/상세 의미를 볼 수 있는 화면.
- **제약**: 카드 데이터는 `GET /api/cards`를 통해서만 가져온다 (`docs/01` §데이터 접근 원칙 — 단일 경로로 단순화).
- **완료 기준**: 카드 78장이 모두 목록에 표시되고, 하나를 선택하면 상세 의미(정/역방향)를 볼 수 있다.
- **검증 기준**: DB의 `cards` 테이블 행 수가 78개인지 확인 (`docs/02` §시드 데이터).

### FR-8. 리딩 기록 (익명, 로컬 기준)
- **범위**: 브라우저(기기)별 익명 세션 ID(`localStorage`에 저장된 UUID)로 최근 리딩 히스토리를 서버(Supabase)에 저장하고 다시 볼 수 있다.
- **제약**: 로그인 기능은 MVP에 포함하지 않는다(§10 범위 외). 세션 ID는 개인식별정보가 아니어야 한다.
- **완료 기준**: 같은 브라우저로 재방문 시 최근 리딩 목록이 보인다. 다른 브라우저/시크릿 모드에서는 보이지 않는다.
- **검증 기준**: `localStorage` 삭제 후 재방문 시 히스토리가 비어있음을 확인.

## 6. 비기능 요구사항

- **NFR-1 (모바일 우선)**: 375px 기준으로 설계, 터치 타겟 최소 44x44px.
- **NFR-2 (비밀키 보호)**: `OPENAI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`는 서버 코드에서만 사용 (`AGENTS.md` §3.1).
- **NFR-3 (비용 통제)**: 동일 세션 기준 5분당 5회 이상 `POST /api/readings` 요청 시 429 오류 반환 (`docs/03` §레이트리밋).
- **NFR-4 (성능)**: 정적 리소스 초기 로딩 1MB 이하(외부 이미지 CDN 제외), 프레임워크 미사용으로 JS 번들 최소화.
- **NFR-5 (접근성)**: WCAG AA 명도 대비, 모든 인터랙션 요소에 키보드 접근 가능.
- **NFR-6 (안전/윤리)**: 모든 결과 화면 하단에 "본 서비스는 오락 목적이며 의학적·법률적·재정적 조언을 대체하지 않습니다" 문구 고정 표시. 죽음/질병/확정적 재난 등 단정적 부정 예언 표현 금지 (`docs/04` §안전 가이드라인).

## 7. 디자인 레퍼런스 & 방향성

세 가지 참고 이미지를 바탕으로 하되, **그대로 모방하지 않고 요소만 결합해 새로운 톤을 만든다.** (상세 사양은 `docs/05-ui-ux-design-spec.md`)

1. **레퍼런스 A (럭셔리 브랜드 인트로 무드)** — 베이지/크림 톤의 부드러운 조명, 얇은 세리프 서체, 여백을 넉넉히 쓰는 절제된 타이포그래피, 좌상단/하단에 작은 캡션형 UI 텍스트. → 이 프로젝트에서는 **타이포그래피의 절제미와 여백감**만 차용한다.
2. **레퍼런스 B (신비로운 야간 풍경 인트로)** — 어두운 앰버/골드 톤 그라디언트 하늘, 산등성이 실루엣과 상징적 오브제(피라미드), 빛줄기 연출, "클릭하여 밝히기" 같은 참여 유도 문구, 우상단 메뉴·하단 스크롤/사운드 토글 같은 미니멀 UI 라벨. → 이 프로젝트에서는 **어두운 배경 + 빛 연출 + 참여 유도형 인트로 문구 패턴**을 차용한다.
3. **레퍼런스 C (모바일 앱 바텀시트/필터 UI)** — 하단에서 올라오는 바텀시트, 상단 드래그 핸들 바, 필터/정렬 단일 선택 UI, 검정색 알약형(pill) 전체폭 버튼, 상세 화면의 정보 패널. → 이 프로젝트에서는 **스프레드 선택, 카드 사전 필터, 결과 상세 화면의 인터랙션 패턴**으로 차용한다 (바텀시트, 검정 pill 버튼, 드래그 핸들).

최종 컨셉: **"어두운 밤하늘 + 은은한 금빛 조명" 배경 위에 절제된 세리프 타이포그래피**를 얹고, 실제 조작(스프레드 선택/카드 사전 등)은 **하단 바텀시트와 검정 pill 버튼** 패턴으로 처리한다.

## 8. 기술 스택 & 아키텍처

`AGENTS.md` §1, `docs/01-architecture.md` 참조. (HTML/CSS/JS, Node.js on Vercel Serverless Functions, OpenAI API, Supabase, Vercel 배포)

## 9. 신뢰성 있는 타로 해석 정책 (핵심 차별점)

"리뷰가 좋은 순" 같은 실시간 외부 평판 데이터는 이 프로젝트 범위에서 안정적으로 수집할 수 없으므로, 신뢰성은 다음과 같이 **운영적으로 정의**한다 (상세는 `docs/04-ai-interpretation-guide.md`):

1. 78장 카드 의미는 널리 통용되는 표준 체계(라이더-웨이트 계열 정/역방향 키워드·의미)를 기반으로 사전에 정리해 `data/seed-cards.json`에 시딩한다.
2. 이 데이터는 **최소 2개 이상의 공신력 있는 공개 타로 레퍼런스와 교차 검증**한 뒤 등록한다.
3. AI(OpenAI)는 응답 생성 시 이 시드 데이터를 프롬프트에 "참고자료"로 주입받고, **참고자료 범위를 벗어난 새로운 상징/의미를 창작하지 않도록 시스템 프롬프트로 제약**한다.
4. 모든 결과에 오락 목적 디스클레이머를 표시한다.

## 10. 범위 외 (Out of Scope) — MVP

- 로그인/회원가입, 소셜 로그인
- 결제, 유료 구독, 실제 상담사 매칭
- 다국어 지원 (한국어 단일 언어로 시작)
- 타로 외 콘텐츠 (별자리 운세 등은 "참고 지식"으로 해석 품질을 보강하는 용도로만 내부적으로 쓰일 수 있으나, 별도 화면/기능으로 노출하지 않음)
- 네이티브 앱

이 항목들은 "구현하지 않는다"가 아니라 **"이번 단계에서 만들지 않기로 확정했다"**는 의미이므로, 에이전트는 이 목록에 있는 기능을 임의로 추가하지 않는다.

## 11. 마일스톤

1. **M1 — 기반 구축**: 리포지토리 구조, Supabase 스키마/시드, `.env` 설정 (`docs/01`, `02`, `06`)
2. **M2 — 백엔드**: `/api/cards`, `/api/spreads`, `/api/readings` 구현 (`docs/03`)
3. **M3 — AI 연동**: 그라운딩 프롬프트 및 안전 가이드라인 적용 (`docs/04`)
4. **M4 — 프론트엔드 UI**: 인트로 배경, 스프레드 선택, 카드 덱, 결과 화면, 카드 사전 (`docs/05`)
5. **M5 — 배포 및 검증**: Vercel 배포, 전체 시나리오 QA (`docs/06`)

## 12. 완료 기준 (프로젝트 전체 Definition of Done)

- [ ] FR-1 ~ FR-8 전체가 각 항목의 완료 기준을 충족
- [ ] NFR-1 ~ NFR-6 전체 충족
- [ ] Vercel 프로덕션 URL에서 전체 플로우(인트로 → 스프레드 선택 → 카드 뽑기 → 결과 → 카드 사전)가 오류 없이 동작
- [ ] `AGENTS.md` §6 공통 DoD 충족

## 13. 검증 및 승인 기준 (Acceptance Criteria)

- [ ] 모바일(375px), 태블릿(768px), 데스크톱(1280px) 3개 뷰포트에서 수동 QA 통과
- [ ] 동일 스프레드로 10회 리딩 시 카드 조합이 매번 달라짐 (난수 검증)
- [ ] AI 해설 샘플 5건 이상에서 `docs/04` 참고자료 범위를 벗어난 내용 없음
- [ ] 브라우저 Network 탭에서 `OPENAI_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY` 노출 없음
- [ ] `POST /api/readings` 레이트리밋(5분당 5회) 동작 확인

## 14. 리스크 및 열린 질문 (Open Questions)

명시적으로 남겨두는 이유: 에이전트가 임의로 답을 추측해 구현하지 않도록 하기 위함. 기본값이 있는 경우 "기본값"을 표기했고, 별도 지시가 없으면 이 기본값대로 진행한다.

| 질문 | 기본값 (지시 없을 시) |
|---|---|
| 카드 이미지: 실제 78장 일러스트를 쓸 것인가, 상징 아이콘/기하학적 대체 이미지를 쓸 것인가? | 저작권 이슈를 피하기 위해 **자체 제작 SVG 기하학적 카드 디자인**을 기본으로 한다 (`docs/05` §카드 비주얼). |
| 브랜드명/타이틀 확정 | "루나타로" 가칭 유지, 실제 서비스명 확정 전까지 코드에는 `SITE_NAME` 환경변수/상수로 분리해 쉽게 교체 가능하게 한다. |
| Phase 2(로그인, 다국어 등) 착수 시점 | 미정 — MVP 배포 후 사용자 피드백을 보고 별도 논의. |
| OpenAI 모델 종류 | 비용 효율 모델(`docs/04` §모델 선택 기본값) 사용, 필요 시 환경변수로 교체. |
